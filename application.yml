spring:
  application.name: rag-assistant
  datasource:
    url: jdbc:mysql://localhost:3306/rag_db
    username: root
    password: ${DB_PASSWORD:123456}
  jpa.hibernate.ddl-auto: update
  data.redis:
    host: localhost
    port: 6379

ai:
  api-key: ${QWEN_API_KEY}
  base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
  model: qwen-turbo

server.port: 8080
